apiVersion: 1

datasources:
  - name: Prometheus
    # Pin uid so dashboard JSON can reference it stably across re-provisions.
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
